apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: staging-todos-api
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/Sintratel-Docket/gitops-manifests.git
    targetRevision: main
    path: staging/todos-api
  destination:
    name: docket-staging
    namespace: staging-todos-api
  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
