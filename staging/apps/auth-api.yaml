apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: staging-auth-api
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/Sintratel-Docket/gitops-manifests.git
    targetRevision: main
    path: staging/auth-api
  destination:
    name: docket-staging
    namespace: staging-auth-api
  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
