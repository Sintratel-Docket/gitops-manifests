apiVersion: v1
kind: Service
metadata:
  name: auth-api
  namespace: dev-auth-api
spec:
  type: ClusterIP
  selector:
    app: auth-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
