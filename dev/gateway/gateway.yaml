apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: docket-dev-gateway
  namespace: dev-frontend
spec:
  gatewayClassName: docket-alb
  infrastructure:
    parametersRef:
      group: gateway.k8s.aws
      kind: LoadBalancerConfiguration
      name: docket-dev-alb-config
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        kinds:
          - group: gateway.networking.k8s.io
            kind: HTTPRoute
        namespaces:
          from: Selector
          selector:
            matchLabels:
              environment: dev
    # TLS: el ALB termina HTTPS con el certificado ACM definido en el
    # LoadBalancerConfiguration (listenerConfigurations, protocolPort HTTPS:443).
    - name: https
      protocol: HTTPS
      port: 443
      tls:
        mode: Terminate
      allowedRoutes:
        kinds:
          - group: gateway.networking.k8s.io
            kind: HTTPRoute
        namespaces:
          from: Selector
          selector:
            matchLabels:
              environment: dev
